[pytest]
pythonpath = src tests
testpaths = tests
addopts = -m "not retrieval and not llm"
markers =
    retrieval: retrieval eval against Chroma (embedding API; slow)
    llm: full RAG / pipeline tests that call chat LLM (slow)
